@page
@model RechercheInformation.Pages.BooleanModel
@{
    ViewData["Title"] = "Boolean Search";
}

<div class="text-center">
    <h1>Boolean Search </h1>
    <form method="get">
        <label for="query">Enter your query (use AND/OR):</label>
        <input type="text" 
       id="query" 
       name="query" 
       class="form-control steam-input" 
       value="@Model.Query" 
       placeholder="Enter your query" 
       required>
        <input type="submit" value="Search" class="btn btn-lg btn-primary m-3">
    </form>

    @if (Model.SearchResults != null && Model.SearchResults.Any())
    {
        <h2>Search Results:</h2>
        <ul>
            @foreach (var result in Model.SearchResults)
            {
                <li>@result</li>
            }
        </ul>
    }
    else if (!string.IsNullOrEmpty(Model.Query))
    {
        <p>No results found for "@Model.Query".</p>
    }
</div>
